#include<iostream>
using namespace std;
void toh(int n,char from,char to,char aux){
     if(n==0)
     return 0;
     else{
          toh(n-1,from,
     }
     
}
int main(){
   int n;
   cout<<"enter the number of discs=";
   cin>>n;
   toh(n,'A','C','B');
   return 0;
}
